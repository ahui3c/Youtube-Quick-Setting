# YouTube 快速設定速度 / 畫質 v1.2.0

## 新功能

- YouTube Shorts 按 `←` 倒退 5 秒、按 `→` 快進 5 秒。
- Shorts 按 `0` 或數字鍵盤 `0` 回到片頭。
- 快進、倒退與回到片頭時顯示時間提示。
- 長按左右方向鍵可連續跳轉。
- 在搜尋、留言、表單與播放器選單操作時不攔截方向鍵。
- 一般影片維持 YouTube 原生左右方向鍵與數字鍵行為。

## 介面

- Shorts 設定頁顯示完整快捷鍵提示。
- 繁體中文、英文與日文提示同步更新。
