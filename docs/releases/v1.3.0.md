# YouTube 快速設定速度 / 畫質 v1.3.0

## 新功能

- Shorts 快進與倒退秒數可選 `3 秒`、`5 秒`（預設）或 `10 秒`。
- 可獨立開啟或關閉 Shorts 的左右方向鍵。
- 關閉方向鍵後，`0` 與數字鍵盤 `0` 仍可回到片頭。

## 相容性

- 舊版設定會自動補上 `5 秒`與「方向鍵開啟」，不需要重新設定。
- 一般影片的方向鍵與數字鍵行為不受影響。
- 設定仍只儲存在 Chrome 同步儲存空間，不增加任何權限。

## 介面

- Shorts 設定頁加入跳轉秒數選擇與方向鍵開關。
- 繁體中文、英文與日文介面及商店說明同步更新。
